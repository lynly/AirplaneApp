var app = app || {};

app.ReservationInputView = Backbone.View.extend({

  // el: "target div id#",

  events: {

  },

  // Functions from events
  // E.g. create reservation
  // Fire the add event

render: function () {
// add the template mark up to the target element 
}


});
